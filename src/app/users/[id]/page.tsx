const DetailUserPage = (props: any) => {
  const { params } = props;
  return (
    <div>
      not fill id = {params?.id}
      {/* slug= {slug} */}
    </div>
  );
};

export default DetailUserPage;
